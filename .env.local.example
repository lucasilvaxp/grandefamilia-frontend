# Fashion Catalog Environment Variables

# Set to 'true' to use mock data instead of FastAPI backend (useful for development)
USE_MOCK_DATA=true

# FastAPI Backend URL (when not using mock data)
# NEXT_PUBLIC_FASTAPI_URL=http://localhost:8000
